import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header className="header">
      {/* Contenido del encabezado */}
    </header>
  );
};

export default Header;
